'use struct';

const mainApi = config.api;

const axios = {
  GET: function (params) {
    
  },
  POST: function (params) {

  },
  DELETE: function (params) {

  }
}

const paginate = function(page) {
  
}